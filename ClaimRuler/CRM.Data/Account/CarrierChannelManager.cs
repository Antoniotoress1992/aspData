using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using CRM.Data.Entities;

namespace CRM.Data.Account {
	public static class CarrierChannelManager {
		//public static List<CarrierChannel> GetChannels(int carrierID) {
		//	List<CarrierChannel> channels = null;

		//	channels = (from x in DbContextHelper.DbContext.CarrierChannels
		//			  where x.CarrierID == carrierID
		//			  select x).ToList<CarrierChannel>();

		//	return channels;

		//}
	}
}
